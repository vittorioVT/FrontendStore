import { Comment } from '@angular/compiler';

export interface ProductElements {
  Id: number,
  Name: string,
  Description: string,
  Picture: string,
  Color: string,
  Size: string,
  Price: number,
  Quantity: number,
  Comment: string
}
